import { GeneralSearchPayload } from './general-search-payload';

describe('GeneralSearchPayload', () => {
  it('should create an instance', () => {
    expect(new GeneralSearchPayload()).toBeTruthy();
  });
});
