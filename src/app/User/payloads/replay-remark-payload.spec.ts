import { ReplayRemarkPayload } from './replay-remark-payload';

describe('ReplayRemarkPayload', () => {
  it('should create an instance', () => {
    expect(new ReplayRemarkPayload()).toBeTruthy();
  });
});
