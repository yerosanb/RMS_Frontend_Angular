import { TotalCurrencyRemarks } from './total-currency-remarks';

describe('TotalCurrencyRemarks', () => {
  it('should create an instance', () => {
    expect(new TotalCurrencyRemarks()).toBeTruthy();
  });
});
