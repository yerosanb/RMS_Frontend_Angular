import { UpdateRemarkPayload } from './update-remark-payload';

describe('UpdateRemarkPayload', () => {
  it('should create an instance', () => {
    expect(new UpdateRemarkPayload()).toBeTruthy();
  });
});
