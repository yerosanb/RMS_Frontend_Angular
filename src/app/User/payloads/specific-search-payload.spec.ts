import { SpecificSearchPayload } from './specific-search-payload';

describe('SpecificSearchPayload', () => {
  it('should create an instance', () => {
    expect(new SpecificSearchPayload()).toBeTruthy();
  });
});
